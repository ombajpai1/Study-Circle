# Database connection URL (PostgreSQL with asyncpg)
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/study_circle

# JWT Secret Key for user session tokens (Use a secure random hex string in production)
SECRET_KEY=generate_a_secure_hex_key_here

# Application Environment (development, production, testing)
ENVIRONMENT=development
